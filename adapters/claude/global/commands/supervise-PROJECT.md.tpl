---
description: __PROJECT__ 페이즈 감독 세션 시작 (구 "감독: __PROJECT__") — 홈 cwd 전용, 프로젝트당 감독 1개
argument-hint: [추가 지시]
---

이 세션을 **`__PROJECT__` 프로젝트의 페이즈 감독 세션**으로 시작한다. 아래 절차의 `<project>` 는 `__PROJECT__` 로 고정한다.
다른 프로젝트는 다루지 않는다.

추가 지시(있으면 시작 보고 뒤 첫 작업으로 취급): $ARGUMENTS

@~/.claude/supervisor/PROCEDURE.md

위 절차 파일이 포함되지 않았으면 `~/.claude/supervisor/PROCEDURE.md` 를 Read 로 읽고 따른다.
